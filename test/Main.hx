package test;
import haxe.Timer;
import odd.Engine;
import odd.ImageBuffer;

/**
 * ...
 * @author
 */
class Main extends Engine
{
    static function main() 
    {
        new Main(640, 480, 10);
    }
}